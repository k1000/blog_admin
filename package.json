{
  "name": "blog_admin",
  "version": "0.0.0",
  "main": "dist/index.js",
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0",
    "@cloudflare/workers-types": "^4.20221111.1",
    "@types/marked": "^4.0.8",
    "better-sqlite3": "^7.6.2",
    "typescript": "^4.9.4",
    "vitest": "^0.27.3",
    "wrangler": "^2.9.0"
  },
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --outdir=dist ./src/index.tsx",
    "dev": "miniflare --live-reload --debug",
    "dev:db": "wrangler dev --local --persist",
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^2.7.5",
    "marked": "^4.2.12",
    "tsx-create-element": "^2.2.9"
  }
}
